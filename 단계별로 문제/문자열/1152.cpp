#include <iostream>
#include <string>

typedef long long int lli;

using namespace std;

void solve() {
    string s;
    int r = 0;
    while (cin >> s) {r++;}
    cout << r << "\n";
}

int main(void) {
    cin.tie(NULL);
    ios_base::sync_with_stdio(false);
    solve();
}